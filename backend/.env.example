# Backend configuration for local development
# Copy to .env (these values work out of the box with docker-compose.yml)

# Database connection (PostgreSQL)
DATABASE_URL=postgres://gastos:gastos_dev_password@localhost:5432/gastos?sslmode=disable

# Server address (default port 8080)
# SERVER_ADDR=:8080

# Session settings
# Cookie name for session (default: gastos_session)
# SESSION_COOKIE_NAME=gastos_session
# Whether to secure the session cookie (default: true in production thus false here for local dev)
SESSION_COOKIE_SECURE=false

# Rate limiting (enabled by default for security)
# Set to "false" ONLY for testing/development to avoid rate limit errors
# NEVER disable in production
RATE_LIMIT_ENABLED=false

# CORS - allowed origins (comma-separated)
# Not needed if using the recommended setup (backend serving frontend at same origin)
# See docs/DEVELOPMENT.md for details
# Only set this if running frontend separately (e.g., python3 -m http.server 8000)
# ALLOWED_ORIGINS=http://localhost:8000

# Serve frontend static files (for local development)
STATIC_DIR=../frontend

# Email configuration
# Provider options: "noop" (default, logs only) or "smtp" (local testing)
EMAIL_PROVIDER=noop
EMAIL_FROM_ADDRESS=noreply@gastos.blanquicet.com.co
EMAIL_FROM_NAME=Gastos
EMAIL_BASE_URL=http://localhost:8080

# SMTP configuration (for local email testing with Mailtrap)
# Sign up at mailtrap.io for free email testing
# Uncomment and configure to test email sending locally:
# SMTP_HOST=sandbox.smtp.mailtrap.io
# SMTP_PORT=587
# SMTP_USERNAME=441d2a05be8320
# SMTP_PASSWORD=284731a52efac8
